[openresty-noroot]
plan_path = "src/openresty-noroot"

[oc_id]
plan_path = "src/oc-id"
export_targets = ["docker"]

[chef-server-nginx]
plan_path = "src/nginx"
export_targets = ["docker"]

[bookshelf]
plan_path = "src/bookshelf"
export_targets = ["docker"]

[cinc-server-ctl]
plan_path = "src/cinc-server-ctl"
paths = [
  "src/cinc-server-ctl/*",
  "oc-chef-pedant/*"
]
export_targets = ["docker"]

[oc_bifrost]
plan_path = "src/oc_bifrost"
export_targets = ["docker"]

[oc_erchef]
plan_path = "src/oc_erchef"
export_targets = ["docker"]
